#include "main.h"
#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>
int _printf(const char *format, ...)
{
va_list args;
int compteur;
int compteur 2;
va_start(args, format)
for (compteur =0; format[compteur] != '\0'; compteur++);
if format == NULL
return (NULL);
if format [compteur]=='%'
compteur++;
for (format[compteur]!='\0'; compteur++)
    for (compteur2 = 0; tableau.Spéciale[compteur2]!='\0';compteur2++);
    if compteur == compteur2
    write(1, &c, 1)
    else return (NULL);
